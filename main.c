#include "main.h"

/**
 * main -
 * @ac:
 * @av:
 *
 * Return:
*/

int main(void)
{
	char *input;
	prompt();

	line = readline();

	if (!line)
	{
		continue;
	}
	else
	{
		return (0);
		free(line);
	}
	execmd(args)
}
